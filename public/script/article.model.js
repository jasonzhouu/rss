angular.module('rss')
    .factory('ArticleModel', function ($http, ArticlesListModel) {
        let model = {};

        // model.article = ArticlesListModel.article;
        
        return model;
    });